#!/usr/bin/env Rscript

# CLI entrypoint (planned)
# Usage (planned):
#   Rscript scripts/cli.R --config config.yml

args <- commandArgs(trailingOnly = TRUE)
cat("CLI not implemented yet. Args:\n")
print(args)
